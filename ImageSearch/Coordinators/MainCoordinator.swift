import UIKit

class MainCoordinator: Coordinator {
    enum Destination {
        case local, global
    }

    func start() {
    }

    func navigate(to: Destination) {
    }

    func handleError(with: String) {
    }
}
